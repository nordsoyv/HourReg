using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using NUnit.Framework;
using TimeForing.Models;

namespace TimeRegTest
{
    [TestFixture]
    public class DbMove
    {
        [Test]
        public void CreateAssingments()
        {
        /*    TimeforingRepository repo = new TimeforingRepository();
            var tasks = repo.FindAllTasks();
            foreach (var task in tasks)
            {
                var ass = new Assignment();
                ass.SprintID = task.SprintID ?? 1;
                ass.TaskID = task.TaskID;
                ass.UserID = task.UserID ?? 1;
                repo.AddAssignments(ass);
            }*/
        }

    }
}
